namespace TestApp.Models.Enums;

public enum PassType
{
    Passed,
    Failed
}

public enum AskType
{
    SingleCorrect,
    MultipleCorrect
}

public enum AnswerCorrectness
{
    Correct,
    Incorrect
}

public enum RoleType
{
    Admin,
    Teacher,
    Student
}